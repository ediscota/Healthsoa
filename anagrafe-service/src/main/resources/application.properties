spring.application.name=anagrafe-service
spring.profiles.active=dev
spring.config.import=optional:configserver:http://${CONFIG_SERVER_HOST:localhost}:${CONFIG_SERVER_PORT:8888}

# CXF pubblica il servlet su /soap; l'endpoint viene montato su /soap/anagrafe
cxf.path=/soap
